1. Data loading and splitting
   Load the data from the file data/daneXX.txt
   Propose and implement a division of the data into training and test sets.
2. Neural network design and training
   Propose an optimal neural network to approximate the loaded data.
   Implement Your own neural network model using numpy.
   Use a non-linear activation function in the hidden layer (e.g., tanh or sigmoid).
   Train the network using batch backpropagation.
3. Network evaluation – overfitting/underfitting
   Assess the network performance in terms of fitting: underfitting, optimal fit, or overfitting.
4. Change training method
   Switch from batch training to online (stochastic) training.
   Retrain the network and evaluate its performance.
5. RELU activation function
   Design a network using the RELU activation function in the hidden layer.
   Train the network with batch backpropagation.